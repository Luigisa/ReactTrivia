import React from "react";

const divStyle = {
  textAlign: "center",
  width: "100%"
};

const Header = () => (
  <div style={divStyle}>
    <img src="/img/REACT.JS.png" alt="Logo" style={divStyle} />
  </div>
);

export default Header;
